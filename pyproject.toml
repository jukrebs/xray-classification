# =====================================================================
# For a full TOML configuration guide, check the Flower docs:
# https://flower.ai/docs/framework/how-to-configure-pyproject-toml.html
# =====================================================================

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "berlin25-xray"
version = "1.0.0"
description = ""
license = "Apache-2.0"
requires-python = ">=3.9,<3.13"
dependencies = [
    "flwr[simulation]>=1.22.0",
    "ray>=2.0.0",
    "torch",
    "torchvision",
    "datasets",
    "scipy",
    "scikit-learn",
    "wandb",

    # Analysis and visualization
    "jupyterlab",
    "marimo",
    "pandas",
    "matplotlib",
    "seaborn",
]

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.flwr.app]
publisher = "hackathon"

# Point to your ServerApp and ClientApp objects
[tool.flwr.app.components]
serverapp = "berlin25_xray.server_app:app"
clientapp = "berlin25_xray.client_app:app"

# Custom config values accessible via `context.run_config`
[tool.flwr.app.config]
image-size = 224        # ViT-B/16 pretraining resolution
num-server-rounds = 1   # Single round (Hospital A only run)
local-epochs = 10       # Equivalent to 10 total epochs per round
lr = 0.0001             # Head-only finetuning LR

# Default federation to use when running the app
[tool.flwr.federations]
default = "cluster"

# Local simulation federation with a single SuperNode (Hospital A only)
[tool.flwr.federations.local-simulation]
options.num-supernodes = 1

[tool.flwr.federations.supergrid-deployment]
address = "supergrid.flower.ai"
enable-account-auth = true

[tool.flwr.federations.cluster]
# Cluster deployment limited to one supernode (Hospital A only)
options.num-supernodes = 1
options.backend.client-resources.num-cpus = 4
options.backend.client-resources.num-gpus = 0.33
